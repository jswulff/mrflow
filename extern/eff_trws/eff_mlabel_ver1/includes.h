#ifndef __INCLUDES_H
#define __INCLUDES_H

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#define NOLABEL 255
#define MAX_EDGE_CAP 50000

typedef long captype;
typedef long tcaptype;
typedef long flowtype;

#endif
